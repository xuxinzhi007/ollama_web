# 🎯 LoRA 微调 - 3步操作指南

## 🚀 新手必看（30秒上手）

### 第1步：激活环境
```bash
cd /Users/admin/Documents/ollama_web/finetune
source .venv/bin/activate
```

### 第2步：选择操作方式

#### 🔥 方式A：图形化菜单（推荐新手）
```bash
./quick_start.sh
```
然后按提示选择即可！

#### ⚡ 方式B：直接命令（推荐熟练用户）
```bash
# 训练新模型
python train_to_ollama.py --ollama_name "我的助手"

# 批量导入
python scripts/ultimate_solution.py --batch
```

### 第3步：测试使用
```bash
# 查看模型
ollama list

# 测试对话
ollama run 我的助手
```

**就这么简单！** 🎉

---

## 📋 核心文件说明

| 文件 | 用途 | 何时使用 |
|-----|------|---------|
| `train_to_ollama.py` | 🔥 **主工具** | 训练新模型时 |
| `quick_start.sh` | 📱 图形菜单 | 新手不熟悉命令时 |
| `scripts/ultimate_solution.py` | 🔄 批量管理 | 管理多个模型时 |
| `README.md` | 📖 详细说明 | 需要了解细节时 |

**记住：90% 的时候你只需要用 `train_to_ollama.py`！**

---

## ⚡ 常用命令速查

```bash
# === 训练相关 ===
# 基础训练
python train_to_ollama.py --ollama_name "助手v1"

# 训练3轮
python train_to_ollama.py --ollama_name "助手v2" --epochs 3.0

# 跳过训练，导入现有
python train_to_ollama.py --skip_train --ollama_name "助手v3" --merged_dir "out/merged"

# === 管理相关 ===
# 查看模型
ollama list

# 测试模型
echo "你好" | ollama run 助手v1

# 删除模型
ollama rm old-model

# 批量导入所有训练模型
python scripts/ultimate_solution.py --batch
```

---

## 🔧 故障排查

### ❌ 虚拟环境问题
```bash
# 重新创建环境
rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### ❌ 训练失败
```bash
# 检查环境
python env_detect.py

# 清理重来
rm -rf out/
python train_to_ollama.py --ollama_name "test"
```

### ❌ Ollama问题
```bash
# 重启Ollama服务
killall ollama
ollama serve &
```

---

## 💡 最佳实践

1. **命名规范**：用有意义的名称，如 `chatbot-v1.2`、`code-helper` 等
2. **定期清理**：`ollama list` 查看，删除不需要的模型
3. **备份重要模型**：复制 `out/` 目录到其他地方
4. **测试对比**：训练后对比原模型和微调模型的效果

---

## 🎯 总结

**记住这一个命令就够了：**
```bash
python train_to_ollama.py --ollama_name "你的模型名"
```

其他都是可选的辅助功能！ 🚀